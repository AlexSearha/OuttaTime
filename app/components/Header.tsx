import React from "react";
import NavMenu from "./NavMenu";

const Header = () => {
  return (
    <header className="h-20 bg-red-300">
      <NavMenu />
    </header>
  );
};

export default Header;
